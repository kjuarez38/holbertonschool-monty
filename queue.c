#include "monty.h"

/**
 * queue - sets the formats of the data to queue (FIFO)
 * @stack: Pointer to the stack
 * @line_number: Line number where the mul function is called
 */
void queue(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
	arguments->stack = 0;
}
