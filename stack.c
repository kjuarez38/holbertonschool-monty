#include "monty.h"

/**
 * stack sets the format of the data to a stack (LIFO)
 * @stack: Pointer to the stack.
 * @line_number: to follow the number line of the reading of the files
 */
void stack(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
	arguments->stack = 1;
}
